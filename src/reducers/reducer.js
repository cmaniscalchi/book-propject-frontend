const initialState = {
  searchResults: [],
  selectedBook: {},
  bookshelfBooks: []
}

export default function reducer(state = initialState, action) {
  switch (action.type) {
    // case expression:
    // return object
    default:
      return state
  }
}
